# Практика 1-1: Системные вызовы

## 📝 Описание задания
Реализовать программу, которая использует только системные вызовы (open(), read(), write(), close()) для работы с файлами.
Вариант 1: Создать файл numbers.txt с числами от 1 до 100 (каждое число на новой строке), вычислить их сумму и записать результат в файл sum.txt.

## 🔧 Требования
- Использовать только системные вызовы: open(), read(), write(), close()
- Не использовать функции стандартной библиотеки: fopen(), fprintf(), fscanf()
- Обрабатывать ошибки с помощью perror()
- Компиляция: gcc -Wall -Wextra -o task task.c

## 🚀 Инструкция по запуску
1. Компиляция программы:
   ```bash
   gcc -Wall -Wextra -o task task.c
   ```
2. Запуск программы:
   ```bash
   ./task
   ```
3. Проверка результатов:
   ```bash
   cat numbers.txt
   cat sum.txt
   ```

## 📊 Результаты работы
После выполнения программы создаются два файла:
- numbers.txt — содержит числа от 1 до 100 (по одному на строке)
- sum.txt — содержит сумму всех чисел (5050)

## 🔍 Проверка
Сумма чисел от 1 до 100 вычисляется по формуле:
S = n*(n+1)/2 = 100*101/2 = 5050

## 🐛 Обработка ошибок
Программа проверяет успешность всех системных вызовов и выводит сообщения об ошибках через perror().
